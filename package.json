{
  "name": "poke",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "gen": "envsubst '${D1_DATABASE_ID}' < wrangler.toml.tmpl > wrangler.toml",
    "dev": "bun run gen && wrangler dev",
    "deploy": "bun run gen && wrangler deploy",
    "db:migrate": "bun run gen && wrangler d1 execute poke-db --local --file=schema.sql",
    "db:migrate:prod": "bun run gen && wrangler d1 execute poke-db --remote --file=schema.sql"
  },
  "dependencies": {
    "grammy": "^1.25.0",
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.60.0"
  }
}
